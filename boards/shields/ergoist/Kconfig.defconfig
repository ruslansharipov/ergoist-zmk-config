if SHIELD_ERGOIST_LEFT

config ZMK_KEYBOARD_NAME
    default "Ergoist"

config ZMK_SPLIT_ROLE_CENTRAL
    default y

endif

if SHIELD_ERGOIST_LEFT || SHIELD_ERGOIST_RIGHT

config ZMK_SPLIT
    default y

endif